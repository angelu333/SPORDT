/**
 * ============================================================
 * SporDT — Configuración de Entorno de Desarrollo
 * ============================================================
 * Este archivo es el que Angular usa al correr "ng serve" o "npm run dev".
 * Para producción se reemplaza automáticamente por environment.prod.ts
 * gracias al fileReplacements en angular.json.
 * ============================================================
 */
export const environment = {
    production: false,
    apiUrl: 'http://localhost:3000/api'
};
